services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: font-project:dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    environment:
      PYTHONPATH: /workspace
    command: bash
    tty: true
    stdin_open: true

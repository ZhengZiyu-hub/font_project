# Configs

Configuration files for data, training, and inference should live here.

The current entry points still expose their options through CLI arguments. Move
stable argument sets into this directory as YAML/JSON when experiments become
repeated.
